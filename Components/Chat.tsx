import React, { FC } from 'react';

type Props = {
    chatId: string
};

const Chat: FC<Props> = ({ chatId }) => {
    return (
        <div className="flex-1">
            
        </div>
    )
};

export default Chat;